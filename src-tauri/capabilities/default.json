{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities: store plugin for persistent data, core window controls",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default"
  ]
}
